# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Capacitor native projects (Epic 10 spike). Generated by `npx cap add ios` /
# `npx cap add android` and regenerable from capacitor.config.ts + dist/, so they
# are not tracked yet. Commit them once the wrap graduates past a spike and the
# native shells start carrying real customizations (Info.plist, signing, manifest).
/android
/ios

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
